syntax = "proto3";

package testp;

option go_package="github.com/josemqz/SistDistribuidos/test/testp;testp";

message MSG{
    string ok = 1;
    bytes arch = 2;
}
message ACK{
    string ok = 1;
}

service TestpService{
    rpc RecibirBytes(stream MSG) returns(ACK) {}
}
